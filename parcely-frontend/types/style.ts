// Shared style primitives. Block styles are overrides layered on the storefront
// style, so both draw from the same font/alignment sets.

export type FontFamily = "Inter" | "Roboto" | "Playfair Display" | "Roboto Mono";

export type Alignment = "left" | "center" | "right";
